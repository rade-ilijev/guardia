<?xml version="1.0" encoding="utf-8"?>
<!--
    Play-flavor accessibility config: the minimum needed to detect the foreground app for per-app
    face checks / App Lock and tamper detection. Deliberately drops canTakeScreenshot,
    canRetrieveWindowContent and flagRetrieveInteractiveWindows — the capabilities reviewers scrutinize
    most — so the store build cannot read or capture screen content. The per-app check therefore uses
    the opaque "Loading" style only (blur/freeze are full-flavor).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:isAccessibilityTool="true"
    android:notificationTimeout="100"
    android:settingsActivity="com.guardia.app.MainActivity" />
